{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python -m unittest discover -s tests -t . && python -m engine.report data/buildings/esquisse-gasan tests/golden/G2 tests/golden/G3 --out build --dashboard public/index.html"
  },
  "deploy": {
    "startCommand": "python -m http.server $PORT --directory public",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
